generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Persona {
  id        Int     @id @default(autoincrement())
  nombre    String
  apellido  String
  documento String  @unique
  direccion String?
}
