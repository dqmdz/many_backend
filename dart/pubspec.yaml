name: persona_crud
description: CRUD de personas con Dart y SQLite
version: 1.0.0

executables:
  persona_crud:
    script: bin/main.dart

environment:
  sdk: ^3.0.0

dependencies:
  drift: ^2.14.0
  sqlite3: ^2.4.0
  xdg_directories: ^1.1.0
  path: ^1.8.3
  shelf: ^1.4.1
  shelf_router: ^1.1.4

dev_dependencies:
  drift_dev: ^2.14.0
  build_runner: ^2.4.7
